# Language setting
language: java



# Version number
jdk:
  #- openjdk7
  - openjdk8
  #- openjdk9
  - oraclejdk8
  - oraclejdk9
env:
  global:
    - ENVG: "goo"
  matrix:
    - ENV1: "foo"
    - ENV2: "bar"
    - ENV1: "boo"
    - ENV2: "car"

matrix:
  exclude:
    - jdk: oraclejdk9
      env: ENV1=foo ENV2=bar
build:
   

    
  ci:
    - java -version  
    - echo $SHIPPABLE_JDK_VERSION
    - sudo docker images
    - mvn --version
    - gradle --version 
    - ant -version  
    


